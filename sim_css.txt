
/* ─── SIMULATOR LAB ─────────────────────────────────── */
.sim-header { margin-bottom: 12px; }
.sim-scenarios { display: flex; flex-direction: column; gap: 8px; }
.sim-btn {
  display: flex;
  align-items: center;
  background: var(--surface);
  border: 1px solid var(--border);
  border-radius: var(--radius);
  padding: 10px 12px;
  cursor: pointer;
  text-align: left;
  transition: transform 0.1s, border-color 0.2s;
}
.sim-btn:hover { border-color: rgba(255,255,255,0.2); transform: translateY(-1px); }
.sim-btn:active { transform: translateY(1px); }
.sim-btn-icon { font-size: 20px; margin-right: 12px; }
.sim-btn-title { color: var(--text); font-weight: 600; font-size: 13px; }
.sim-btn-desc { color: #94A3B8; font-size: 10px; margin-top: 2px; }
.sim-btn.safe:hover { border-color: var(--safe); box-shadow: 0 0 8px rgba(16,185,129,0.2); }
.sim-btn.low:hover { border-color: var(--medium); box-shadow: 0 0 8px rgba(245,158,11,0.2); }
.sim-btn.crit:hover { border-color: var(--crit); box-shadow: 0 0 8px rgba(239,68,68,0.2); }

